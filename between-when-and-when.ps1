Import-Module ActiveDirectory; $startDate = Read-Host "Enter start date (MM/DD/YYYY)"; $endDate = Read-Host "Enter end date (MM/DD/YYYY)"; $startDate = [DateTime]::ParseExact($startDate, "MM/dd/yyyy", $null); $endDate = [DateTime]::ParseExact($endDate, "MM/dd/yyyy", $null); Get-ADUser -Filter {Created -ge $startDate -and Created -lt $endDate} -Properties Created | Select-Object Name, SamAccountName, Created
#v9.12//9/10/2023
